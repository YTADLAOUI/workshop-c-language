#include<stdio.h>
#include<stdlib.h>
int main (){
    puts("Ecrivez un programme pour vérifier si le nombre donné est positif, négatif ou nul.");
    float a;
    scanf ("%f",&a);
    if (a>0)
        puts("positif");
    else if (a=0)
       puts("null");
    else if (a<0);
   puts ("negative");

return 0;
}
