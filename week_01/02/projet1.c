#include<stdio.h>
#include<stdlib.h>
#include<math.h>
 int main (){
 	int a ;
 	scanf ("%d",&a);
 	if (a % 2 != 0)
 	 printf("le nombre est impairs");
 	else
 	  	printf("le nombre est pairs");

 	 
 	 return 0;
 	
 }
